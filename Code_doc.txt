# Optional: standardize names before joining
for col in ['Primary_first_name', 'Primary_family_name']:
    df[col] = (
        df[col]
        .astype(str)
        .str.strip()
        .str.upper()
    )

for col in ['CustomerStAddressFirstName', 'CustomerStAddressLastName']:
    df_final[col] = (
        df_final[col]
        .astype(str)
        .str.strip()
        .str.upper()
    )

# Left join
df_merged = df.merge(
    df_final,
    how='left',
    left_on=['Primary_first_name', 'Primary_family_name'],
    right_on=['CustomerStAddressFirstName', 'CustomerStAddressLastName']
)

citadel_cols = [
    'CustomerStAddressFirstName',
    'CustomerStAddressLastName',
    'Application_Number',
    'Application_Date',
    'Data_Source'
]

df_merged = df.merge(
    df_final[citadel_cols],
    how='left',
    left_on=['Primary_first_name', 'Primary_family_name'],
    right_on=['CustomerStAddressFirstName', 'CustomerStAddressLastName']
)

df_final = (
    df_final.sort_values('Application_Date', ascending=False)
            .drop_duplicates(
                subset=['CustomerStAddressFirstName',
                        'CustomerStAddressLastName'],
                keep='first'
            )
)