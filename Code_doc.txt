import pandas as pd
from pathlib import Path

txt_folder_path = Path(r"C:\your\txt\folder\path")

# Read all pipe-separated txt files
txt_files = list(txt_folder_path.glob("*.txt"))

txt_dfs = {}

for file in txt_files:
    df_name = file.stem
    txt_dfs[df_name] = pd.read_csv(file, sep="|", dtype=str)

# Combine all txt files into one dataframe
txt_all = pd.concat(txt_dfs.values(), ignore_index=True)

# Merge with the earlier merged dataset
final_df = merged_df.merge(
    txt_all,
    on="unique_record_ID",
    how="left"
)

print(final_df.shape)
print(final_df.head())